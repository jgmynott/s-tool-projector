# Research-only deps (sentiment collection, backtests). Not installed on
# the Railway API container. Install locally for backtests:
#   pip install -r requirements-research.txt

# Public Pulse (Google Trends)
pytrends>=4.9.3

# Sentiment collector (WSB FinBERT) — tilts zeroed 2026-04-15, kept for
# reproducibility of the dead-signal forensic
transformers>=5.6.1
torch>=2.1
